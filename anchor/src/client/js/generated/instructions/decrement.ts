/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type WritableAccount,
} from 'gill';
import { PROJECT2_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const DECREMENT_DISCRIMINATOR = new Uint8Array([
  106, 227, 168, 59, 248, 27, 150, 101,
]);

export function getDecrementDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(DECREMENT_DISCRIMINATOR);
}

export type DecrementInstruction<
  TProgram extends string = typeof PROJECT2_PROGRAM_ADDRESS,
  TAccountProject2 extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountProject2 extends string
        ? WritableAccount<TAccountProject2>
        : TAccountProject2,
      ...TRemainingAccounts,
    ]
  >;

export type DecrementInstructionData = { discriminator: ReadonlyUint8Array };

export type DecrementInstructionDataArgs = {};

export function getDecrementInstructionDataEncoder(): FixedSizeEncoder<DecrementInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: DECREMENT_DISCRIMINATOR })
  );
}

export function getDecrementInstructionDataDecoder(): FixedSizeDecoder<DecrementInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getDecrementInstructionDataCodec(): FixedSizeCodec<
  DecrementInstructionDataArgs,
  DecrementInstructionData
> {
  return combineCodec(
    getDecrementInstructionDataEncoder(),
    getDecrementInstructionDataDecoder()
  );
}

export type DecrementInput<TAccountProject2 extends string = string> = {
  project2: Address<TAccountProject2>;
};

export function getDecrementInstruction<
  TAccountProject2 extends string,
  TProgramAddress extends Address = typeof PROJECT2_PROGRAM_ADDRESS,
>(
  input: DecrementInput<TAccountProject2>,
  config?: { programAddress?: TProgramAddress }
): DecrementInstruction<TProgramAddress, TAccountProject2> {
  // Program address.
  const programAddress = config?.programAddress ?? PROJECT2_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    project2: { value: input.project2 ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [getAccountMeta(accounts.project2)],
    programAddress,
    data: getDecrementInstructionDataEncoder().encode({}),
  } as DecrementInstruction<TProgramAddress, TAccountProject2>;

  return instruction;
}

export type ParsedDecrementInstruction<
  TProgram extends string = typeof PROJECT2_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    project2: TAccountMetas[0];
  };
  data: DecrementInstructionData;
};

export function parseDecrementInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedDecrementInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 1) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      project2: getNextAccount(),
    },
    data: getDecrementInstructionDataDecoder().decode(instruction.data),
  };
}
